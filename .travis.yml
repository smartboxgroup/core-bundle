language: php

php:
  - 7.0

env:
  - SYMFONY_VERSION=2.7.*

before_script:
  - composer self-update
  - composer install

script:
  - bin/phpunit --coverage-text --debug

notifications:
  email: true